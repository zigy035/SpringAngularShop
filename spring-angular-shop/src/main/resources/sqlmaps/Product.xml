<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="productMap">
	
	<resultMap id="productResultMap" type="com.spring.angular.shop.model.Product">
		<result property="id" column="id" jdbcType="VARCHAR" />
		<result property="name" column="name" jdbcType="VARCHAR" />
		<result property="description" column="description" jdbcType="VARCHAR" />
		<result property="price" column="price" jdbcType="DECIMAL" />
	</resultMap>

	<select id="getProducts" resultMap="productResultMap">
		SELECT p.id, p.name, p.description, p.price 
		FROM product AS p 
	</select>

	<!-- <select id="getCartProduct" resultMap="productResultMap">
		SELECT p.id, p.name, p.description, p.price 
		FROM product AS p 
			JOIN cart_item AS ci ON ci.product_id = p.id
		WHERE ci.
	</select> -->
	
	
<!-- 	<select id="getItem" resultMap="productResultMap">
		SELECT ci.id, ci.cart_id, ci.quantity 
		FROM cart_item AS ci 
		WHERE ci.id = #{itemId}
	</select>
	
	<update id="updateItem">
		UPDATE cart_item
		SET quantity = #{quantity}
		WHERE id = #{id}
	</update>
	
	<delete id="deleteItem">
		DELETE FROM cart_item
		WHERE id = #{id} 
	</delete>
 -->	
</mapper>